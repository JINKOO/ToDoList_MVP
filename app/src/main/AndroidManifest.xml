<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kjk.todo_sample_mvp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TODO_Sample_MVP">
        <activity
            android:name=".addedittask.AddEditActivity"
            android:exported="false" />
        <activity
            android:name=".tasksdetail.TaskDetailActivity"
            android:exported="false" />
        <activity
            android:name=".tasks.TasksActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TODO_Sample_MVP.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>